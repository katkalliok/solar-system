package solar

object Tests extends App {
  //A test comment
  
  //2 seconds step
  val STEP = 2
  
  //JOTAIN HÄIKKÄÄ NOPEUDEN/KIIHTYVYYDEN KAA.
  
//  val planet = new Planet("Jaa", 20, 5, new Vector3D(2.0, -4.0, -1.0), new Vector3D(20, -5, 10))
//  val p2 = new Planet("Jii", 2, 6, new Vector3D(0, 0, 0), new Vector3D(1, 1, 1))
//  val p3 = new Planet("Mii", 10, 3, new Vector3D(-2, -2, -2), new Vector3D(-1, -1, -1))
//  val neighbors = Vector(p2, p3)
//  val inSpeed = math.sqrt(math.pow(planet.speed.x, 2) + math.pow(planet.speed.y, 2) + math.pow(planet.speed.z, 2))
//  
//  println("Accelaration: " + planet.acceleration(neighbors).toString())
//  println("Our tiny cute planet's travel starts at (" + planet.coords.x + " ; " + planet.coords.y + " ; " + planet.coords.z + ").")
//  println("It's first speed is " + inSpeed  + " m/s.")
//  for (i <- 1 to 5) {
//    planet.updatePlanet(neighbors, STEP)
//    val totalSpeed = math.sqrt(math.pow(planet.speed.x, 2) + math.pow(planet.speed.y, 2) + math.pow(planet.speed.z, 2))
//    println("After " + i + " steps in time, it's proceeded to (" + planet.coords.x + " ; " + planet.coords.y + " ; " + planet.coords.z + ").")
//    println("It's new hasty speed is " + totalSpeed + " m/s.")
//  }
}